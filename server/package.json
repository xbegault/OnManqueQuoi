{
  "name": "server",
  "version": "1.0.0",
  "description": "Server with OnMangeQuoi",
  "main": "index.js",
  "scripts": {
    "start": "npm -g install && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xavier B.",
  "license": "ISC",
  "devDependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.14.0"
  },
  "dependencies": {
    "mongoose": "^4.7.1",
    "nodemon": "^1.11.0"
  }
}
